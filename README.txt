The project file is project.ipynb

The draft file is draft.ypynb where MAINLY cleaning is performed

The .py files contain functions to clean DB. They are not required to run the project, but are required to run the draft file.

The DATA folder contains folder CSV_READY. It contains all the .csv required to run the project.

The other folders and the .nc file are there if required to run the draft file. They are source files, not cleaned.

The GSHHS raw data, due to size, not included.

